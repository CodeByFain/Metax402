# MetaPay402 .npmrc

# Use pnpm for monorepo package management
package-manager=pnpm@9.0.0

# Save exact versions instead of using semver ranges
save-exact=true

# Prefer offline cache to speed up installs
prefer-offline=true

# Automatically install peer dependencies
auto-install-peers=true

# Set default registry (can be replaced with Meta’s internal registry)
registry=https://registry.npmjs.org/

# Prevent package-lock.json creation (pnpm uses its own lockfile)
package-lock=false

# Ensure workspace support
workspaces=true

# Use strict SSL for security
strict-ssl=true

# Cache directory for pnpm
cache=${HOME}/.pnpm-store

# Set custom scope for MetaPay402 packages
@metapay402:registry=https://registry.npmjs.org/

# Include authentication for publishing (optional)
# //registry.npmjs.org/:_authToken=${NPM_TOKEN}

# Logging level
loglevel=warn
